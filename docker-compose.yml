services:
  oblivion-planner:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: oblivion-planner
    ports:
      - '${PORT}:3000'
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    restart: always
    profiles:
      - production

  oblivion-planner-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: oblivion-planner-dev
    environment:
      - WATCHPACK_POLLING=true
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    ports:
      - '${PORT}:3000'
    env_file:
      - .env.local
    restart: always
    profiles:
      - development

volumes:
  node_modules:
